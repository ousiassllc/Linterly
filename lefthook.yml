pre-commit:
  commands:
    format:
      run: test -z "$(gofmt -l .)" || (gofmt -l . && exit 1)
    lint:
      run: golangci-lint run
    test:
      run: go test ./...

pre-push:
  commands:
    build:
      run: go build -o /dev/null ./cmd/linterly
